# tops11

# To run together inside Docker

To build the whole app with docker just use docker-compose up

# To run seperatly

# npm start

To run graphql server from the 'apollo-graphql' folder

runs at localhost:4000
graphql available at /graphql endpoint

# npm run dev

to run front from the 'top11-poc' folder

runs at localhost:3000
